table.table.table-hover
  thead
    tr
      th Name
      th Surname
      th Openned At
      th Starting station
      th Bike
      th 
  tbody
    - @rents.each do |rent|
      tr
        td= rent.user.name
        td= rent.user.surname
        td= rent.openned_at
        td= rent.starting_station.name
        td= rent.bike_id
        td= link_to 'Close', close_station_rent_path(1, rent), method: :post, class: 'btn btn-default'

= render 'open_rent_form'